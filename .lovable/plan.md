

# Add sync comment to App.tsx

Add `// synced` as the first line of `src/App.tsx` — a trivial one-line change that will trigger a rebuild of the preview.

**File:** `src/App.tsx` — prepend `// synced` before the first import.

